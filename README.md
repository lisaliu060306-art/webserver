# Webserver

A simple Python webserver that serves HTML pages (`chess.html`, `add.html`) and handles basic HTTP requests.

## 🚀 About

This project implements a basic webserver using Python.  
It listens for client connections and serves HTML content, handling different routes based on the request.

Features:
- Serves static HTML files (`chess.html`, `add.html`)
- Simple Python handler for routing and responses
- Easy to run and extend

## 🧠 Project Structure
webserver/
├── server.py         # Main Python server script
├── chess.py          # Optional logic for chess interactions
├── chess.html        # HTML page served by the server
├── add.html          # HTML page for add functionality
└── __pycache__/      # Python cache directory
## 🚀 About
This project implements a basic webserver using Python.
It listens for client connections and serves HTML content, handling different routes based on the request.
Features:
Serves static HTML files (chess.html, add.html)
Simple Python handler for routing and responses
Easy to run and extend

## 🛠️ Prerequisites
Python 3 installed
Terminal or command line access
▶️ How to Run
Clone the repository:
- git clone https://github.com/lisaliu060306-art/webserver.git
Navigate into the project:
- cd webserver
Run the server:
- python3 server.py
Open your browser and go to:
- http://localhost:8000
You should see the HTML pages served when navigating to the appropriate routes.

## 🧩 Technologies Used
Python – server logic
HTML – frontend pages

## 📈 Future Improvements
Serve CSS and JS files
Handle POST requests with form data
Add logging
Implement dynamic page templates
